<!--components/day-item.wxml-->
<view class='container'>
  <image src="{{data.url}}" bind:longpress='longPressed' data-item="{{data}}" mode='aspectFill'/>
  <view class='title'>{{data.title}}</view>
</view>
